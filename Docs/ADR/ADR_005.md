# ADR 005: Uso de utilidad de validación en lugar de Value Object para IBAN

## Contexto y Problema

En el diseño inicial de la aplicación se consideró modelar el IBAN como un **Value Object** de DDD para garantizar invariantes de dominio (formato correcto, checksums válidos, inmutabilidad). Sin embargo, al analizar el Excel con datos existentes de familias, se detectó que:

- **Múltiples IBANs en un mismo campo**: Algunos registros contienen 2 o más cuentas bancarias separadas por comas o espacios.
- **Datos sin higienizar**: Formatos inconsistentes, IBANs parciales o valores vacíos.
- **Migración progresiva**: No es viable bloquear la migración hasta limpiar completamente los datos históricos.


## Opciones consideradas

| Opción | Descripción | Pros | Contras |
|--------|-------------|------|---------|
| **A: Value Object IBAN estricto** | `IbanValueObject` con validación en constructor | DDD puro, garantiza invariantes, tipado fuerte | Bloquea migración, rechaza datos existentes |
| **B: String sin validación** | Almacenar como `string` sin restricciones | Acepta cualquier dato, migración inmediata | Sin garantías de calidad, bugs futuros |
| **C: Utilidad de validación opcional** | Helper `IbanValidator.IsValid(string)` + campo `string` | Flexible, permite migración gradual, validación explícita | No fuerza validación en tiempo compilación |

## Decisión

**Implementar utilidad `IbanValidator`** con validación opcional mientras se higienizan los datos:

```csharp
public static class IbanValidator
{
    public static bool IsValid(string iban)
    {
        // Validación formato, checksum, país
    }
}
```

- La columna `IBAN_Completo` se mantiene como `NVARCHAR(50)` sin restricciones.
- Los formularios de **nuevos registros** invocan `IbanValidator.IsValid()` antes de guardar.
- Los datos migrados se marcan con flag `RequiereValidacion = true` para revisión posterior.
- **Roadmap futuro**: Una vez higienizados los datos históricos, se migrará a un Value Object.


## Justificación

- Se ha elegido la opción C porque permite iniciar la aplicación con los datos existentes sin bloquear la migración de familias desde el Excel.
- La opción A (Value Object estricto) rechazaría ~15% de registros con múltiples IBANs o formatos inválidos, retrasando el go-live del sistema.
- La opción B (sin validación) es riesgosa a largo plazo, ya que perpetuaría la entrada de datos incorrectos en nuevos registros.
- La utilidad de validación ofrece un **balance pragmático**: protege la calidad de datos nuevos sin rechazar el histórico.

## Consecuencias

### Positivas

- Migración completa de datos históricos sin pérdida de información, garantizando que todas las familias existentes estén en el sistema desde el inicio.
- Validación robusta en formularios para nuevos IBANs, previniendo errores de entrada y mejorando la calidad de datos futuros.
- Identificación clara de registros que requieren limpieza manual mediante el flag `RequiereValidacion`.
- Evolución gradual hacia un modelo DDD más estricto cuando los datos estén saneados.

### Negativas / Riesgos

- No existe garantía a nivel de modelo de dominio de que un IBAN sea válido, la validación depende de que el desarrollador invoque explícitamente `IbanValidator.IsValid()`.
- Posible inconsistencia temporal con IBANs inválidos en base de datos durante el periodo de higienización de datos históricos.
- Necesidad de proceso manual de limpieza de datos para unificar registros con múltiples IBANs (decidir cuenta principal, separar en registros distintos, etc.).
- Riesgo de que la deuda técnica se perpetúe si no se ejecuta el plan de migración a Value Object una vez completada la higienización.
